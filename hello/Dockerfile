# base image
# <이미지><태그> - 태그 안 붙이면 가장 최신 버전으로
FROM alpine

# 추가적으로 필요한 파일을 다운로드
# 도커 이미지 생성 전 수행할 쉘 명령어
# RUN command

# 컨테이너 시작 시 실행될 명령어 명시
# 컨테이너 시작되었을 때 실행할 파일 및 쉘 스크립트
# DockerFile내 1회만 사용 가능
CMD ["echo", "hello"]